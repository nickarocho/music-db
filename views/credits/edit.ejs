<%- include('../_partials/header') %>
<ul id="slide-out" class="side-nav fixed">
    <li>
        <% if (user.picture) { %>
        <li class="center-align"><img class="profile-pic circle" src="<%= user.picture %>"></li>
        <% } else { %>
        <li class="center-align"><img src="/images/person-placeholder.png" alt="profile-pic"></li>
        <% } %>
    </li>
    <li class="center-align"><h4><%= user.fullName %></h4></li>
</ul>
<div class="navbar-fixed valign-wrapper">
    <nav>
        <a href="#!" class="brand-logo">Music DB</a>
        <div class="nav-wrapper right">
            <ul class=“hide-on-med-and-down right”>
                <li><a href="/logout"><i class="material-icons right">exit_to_app</i>Log Out</a></li>
            </ul>
        </div>
    </nav>
</div>
<main class="credits-page">
    <div class="card black-grey darken-1">
        <div class="card #424242 grey darken-3">
            <h4>Edit This Credit</h4>
            <div id="modal-credit" class="card white lighten-2">
                <div class="row col s12">
                    <div class="col s4 left-align"><p class="modal-content">What did you do?</p></div>
                    <div class="input-field col s3 left-align">
                        <select id="action" name="action">
                            <option value="played" <%= credit.action === 'played' && 'selected' %>>played</option>
                            <option value="wrote" <%= credit.action === 'wrote' && 'selected' %>>wrote</option>
                            <option value="produced" <%= credit.action === 'produced' && 'selected' %>>produced</option>
                        </select>
                    </div>
                    <div class="input-field col s5 left-align">
                        <i class="material-icons prefix">textsms</i>
                        <input type="text" name="instrument" value="<%= credit.instrument %>" id="autocomplete-input" class="autocomplete-instruments">
                        <label for="autocomplete-input">Start typing...</label>
                    </div>
                </div>
                <div class="row col s12 valign-wrapper">
                    <div class="col s3 left-align"><p class="modal-content">Who with?</p></div>
                    <div class="input-field col s9 left-align">
                        <i class="material-icons prefix">textsms</i>
                        <input type="text" name="artist" id="autocomplete-input" value="<%= credit.artist %>" class="autocomplete search-spotify" oninput="searchSpotify()">
                        <label for="autocomplete-input">Start typing... (Powered by Spotify)</label>
                    </div>
                </div>
            </div>
        </div>
        <div class="modal-footer #212121 grey darken-4">
            <a id="add-credit" class="modal-action modal-close waves-effect waves-light btn mdb-submit"><input type="submit" value="Update Credit"></a>
        </div>
    </div>


            <section id="credits-container">
                <form action="/credits/<%= credit._id + '/' + '?_method=PUT'%>" method='post'>
                    <label>Action:
                        <select id="action" name="action">
                            <option value="played" <%= credit.action === 'played' && 'selected' %>>played</option>
                            <option value="wrote" <%= credit.action === 'wrote' && 'selected' %>>wrote</option>
                            <option value="produced" <%= credit.action === 'produced' && 'selected' %>>produced</option>
                        </select>
                    </label>
                    <label>Instrument:
                        <input type="text" name="instrument" value="<%= credit.instrument %>" id="autocomplete-input" class="autocomplete-instruments">
                            <label for="autocomplete-input">Start typing...</label>
                    </label>
                    <label>Artist:
                        <input type="text" name="artist" value="<%= credit.artist %>">
                    </label>
                    <input type="submit" value="Edit">
                </form>
            </section>
            <hr>
            


</main>

    <%- include('../_partials/footer')%>